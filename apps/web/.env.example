# Full API origin in production (e.g. https://api.introbuddy.com). Leave
# unset in local dev -- requests stay relative and vite.config.ts's dev
# server proxy forwards them to apps/api instead.
VITE_API_URL=
